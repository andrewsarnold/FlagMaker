<UserControl x:Class="FlagMaker.Overlays.AttributeSlider"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" d:DesignWidth="300">
    <Grid HorizontalAlignment="Stretch">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="60" />
            <ColumnDefinition Width="40" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>
        
        <Label x:Name="LblName" Grid.Column="0" />
        <Label x:Name="LblValue" Grid.Column="1" Cursor="Hand" MouseUp="Clicked" />
        <TextBox x:Name="TxtValue" Grid.Column="1" KeyDown="TxtValueKeyDown" LostFocus="TxtValueLostFocus" />
        <Slider x:Name="Slider" Grid.Column="2" ValueChanged="SliderValueChanged" />
        <CheckBox x:Name="chkDiscrete" VerticalAlignment="Center" Margin="5,0,0,0" HorizontalAlignment="Stretch"
                  ToolTip="Integer values only" Unchecked="CheckChanged" Checked="CheckChanged" Grid.Column="3" />
    </Grid>
</UserControl>
